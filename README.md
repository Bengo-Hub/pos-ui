# POS UI

BengoBox point-of-sale UI. Order entry, tables, drawer, platform admin.

## Dev

```bash
pnpm install
pnpm dev
```

## Deploy

Deployed at `https://pos.codevertexitsolutions.com`. CI on push to `main`.
